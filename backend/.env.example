# Optional: enables the local AI question endpoints.
OPENAI_API_KEY=
# Optional OpenAI-compatible endpoint; HTTP is accepted only for localhost mock providers.
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4.1-mini
